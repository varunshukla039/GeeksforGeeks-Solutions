class Solution:

    def sameChar(self, A, B):
        # code hereA
        a=A.lower()
        b=B.lower()
        count=0
        for i in range (len(a)):
            if a[i]==b[i]:
                count+=1
        return count
