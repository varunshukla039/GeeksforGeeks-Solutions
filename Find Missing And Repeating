
class Solution:
    def findTwoElement( self,arr, n): 
        res = []
        for a in range(1,n+1):
            res.append(a)

        r = sum(arr)- sum(set(arr))
        m = sum(res)- sum(set(arr))

        return (r,m)
